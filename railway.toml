[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'php artisan migrate --force 2>&1 && php artisan serve --host=0.0.0.0 --port=$PORT 2>&1'"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
